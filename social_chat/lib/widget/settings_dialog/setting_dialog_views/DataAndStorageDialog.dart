import 'package:flutter/material.dart';

class DataAndStorageDialog extends StatelessWidget {
  const DataAndStorageDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
        child: SingleChildScrollView(
      child: Column(
        children: const [
          Text("cec"),
        ],
      ),
    ));
  }
}
