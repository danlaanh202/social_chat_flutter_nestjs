import 'package:flutter/material.dart';

class ChatSettingDialog extends StatelessWidget {
  const ChatSettingDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
        child: SingleChildScrollView(
      child: Column(
        children: [
          Text("cec"),
        ],
      ),
    ));
  }
}
